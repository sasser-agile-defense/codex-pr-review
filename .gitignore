# tree-sitter native bindings — installed by install.sh --version 2 into the
# skill directory, not vendored in the repo.
scripts/node_modules/
scripts/package-lock.json

# OS / editor noise
.DS_Store
*.swp
*~

# Test scratch
/tmp-test/
